# Backend environment variables

# SESSION_TIMEOUT_MINUTES is set in the root .env (project root) and loaded automatically by backend/src/server.js.
# Do NOT duplicate it here. To change the session timeout, edit SESSION_TIMEOUT_MINUTES in the root .env and restart the backend.
# See README.md for details.
